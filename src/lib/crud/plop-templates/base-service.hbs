import { Injectable } from "@nestjs/common";
import { BaseService } from "@/lib/crud";
import { {{pascalCase name}} } from "@/models/{{kebabCase name}}/{{kebabCase name}}";
import { Repository } from "typeorm";
import { InjectRepository } from "@nestjs/typeorm";

@Injectable()
export class {{pascalCase name}}Service extends BaseService<{{pascalCase name}}> {
  constructor(
    @InjectRepository({{pascalCase name}})
    protected readonly {{camelCase name}}Repository: Repository<{{pascalCase name}}>
  ) {
    super("{{pascalCase name}}", {{camelCase name}}Repository);
  }
}

import { Controller } from "@nestjs/common";
import { BaseController } from "@/lib/crud";
import { {{pascalCase name}} } from "@/models/{{kebabCase name}}/{{kebabCase name}}";
import { {{pascalCase name}}Service } from "@/modules/{{kebabCase name}}/{{kebabCase name}}.service";
import { ApiTags } from "@nestjs/swagger";

@Controller("{{pluralize (kebabCase name)}}")
export class {{pascalCase name}}Controller extends BaseController({{pascalCase name}}) {
  constructor(
    protected readonly {{camelCase name}}Service: {{pascalCase name}}Service
  ) {
    super();
  }

  get service() {
    return this.{{camelCase name}}Service;
  }
}

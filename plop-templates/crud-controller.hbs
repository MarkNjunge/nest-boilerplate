import { Controller } from "@nestjs/common";
import { CrudController } from "@/lib/crud";
import {
  {{pascalCase name}},
  {{pascalCase name}}CreateDto,
  {{pascalCase name}}UpdateDto,
} from "@/models/{{kebabCase name}}/{{kebabCase name}}";
import { {{pascalCase name}}Service } from "@/modules/{{kebabCase name}}/{{kebabCase name}}.service";
import { ApiTags } from "@nestjs/swagger";

@Controller("{{pluralize (kebabCase name)}}")
@ApiTags("{{pascalCase name}}")
export class {{pascalCase name}}Controller extends CrudController(
  {{pascalCase name}},
  {{pascalCase name}}CreateDto,
  {{pascalCase name}}UpdateDto
)<{{pascalCase name}}CreateDto, {{pascalCase name}}UpdateDto> {
  constructor(protected readonly {{camelCase name}}Service: {{pascalCase name}}Service) {
    super({{camelCase name}}Service);
  }
}
